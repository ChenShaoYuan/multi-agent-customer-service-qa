# Source code

第二周已实现：

- `schemas`：输入、预检、红线与 State 数据契约；
- `adapters`：正式消息和历史样例适配；
- `preflight`：14项确定性预检；
- `agents`：结构化红线主控；
- `graph`：条件路由与阶段结果；
- `cli.py`：本地演示入口。

第三、四周将增加三个评分 Agent、工具、汇总和评测模块。
